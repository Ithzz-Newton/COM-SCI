```
def main():
    w = input("your weight : ")
    h = float(input("your height : "))

    BMI = weight(w)/height(h)

    # round the BMI to the nearest two decimal points
    # TODO

    print("Your BMI is : ", BMI)

# replace KG to "" and change data type to int
def weight(w):
    # TODO

# height squared calculates
def height(h):
    # TODO

main()

```
